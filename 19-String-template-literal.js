let obj = {
    item: 'Pen',
    price: 20
};
console.log(`The cost of ${obj.item} is ${obj.price} rupees.`);   // best way to write

// OR

console.log("The cost of", obj.item, "is", obj.price, "rupees.");  // good way to write